/***********************************************************************************************
 * Beispiel: Minimales Grundgerüst
 * ===============================
 * 
 * Dies ist das einfachste Programm, dass Sie in C oder C++ schreiben können. Es beinhaltet
 * einfach eine leere `main`-Funktion, die das Betriebssyytem beim Starten des Programms
 * aufrufen kann, die aber nichts macht. Alle unsere Programme werden dieser Vorlage folgen.
 ***********************************************************************************************/

/**
 * Hauptfunktion des Programms. Diese wird vom Betriebssystem aufgerufen, um das Programm
 * zu starten. Die beiden Parameter `argc` und `argv` bekommen die sog. Kommandozeilenparameter
 * (Argumente) übergeben. Als Rückgabewert wird ein optionaler, numerischer Wert erwartet, von dem
 * jeder andere Wert außer null einen Fehler symbolisiert.
 */
int main(int argc, char **argv) {
    // Quellcode hier
}
