/***********************************************************************************************
 * Aufgabe: Preis pro Kilogramm
 * ============================
 * 
 * Um Preisvergleiche im Supermarkt zu erleichtern, steht auf jedem Preisschild nicht nur der
 * tatsächliche Preis des jeweiligen Produkts, sondern klein daneben auch der Vergleichspreis,
 * der auf eine Bezugsgröße wie ein Kilogramm, ein Liter usw. umgerechnet wurde. Kostet eine
 * 500g-Packung Müsli beispielsweise 2,54 €, steht der Vergleichspreis 5,08 € / kg daneben.
 *
 * Schreiben Sie ein Programm, das zwei Werte über die Tastatur abfragt: Den tatsächlichen
 * Preis eines Artikels sowie sein Gewicht. Anschließend soll das Programm den Vergleichspreis
 * ausrechnen und als Ergebnis anzeigen. Der Preis soll mit maximal zwei Nachkommastellen
 * angezeigt werden (setprecision).
 *
 * Achten Sie darauf, Eingabefehler sauber abzufangen. Das Programm soll mit einer sprechenden
 * Fehlermeldung beendet werden, wenn eine grob falsche Eingabe, wie ein Text wo Zahlen erwartet
 * werden, gemacht werden.
 ***********************************************************************************************/

#include <iostream>     // std::cout, std::cin, ...
#include <iomanip>      // std::setprecision
#include <string>       // std::string, std::getline, ...

using namespace std;

int main(int argc, char **argv) {
}